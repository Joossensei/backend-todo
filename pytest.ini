[tool:pytest]
# Only run the lightweight startup test; legacy FastAPI tests have been
# removed as the project now uses AIOHTTP with asyncpg.
testpaths = test_app.py
python_files = test_app.py
norecursedirs = tests
